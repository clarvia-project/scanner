fastapi~=0.135.1
uvicorn[standard]~=0.41.0
aiohttp~=3.13.3
pydantic[email]~=2.12.5
pydantic-settings~=2.13.1

# Bounded in-memory caching — prevents OOM on Render Starter (512MB)
cachetools~=6.2.6

# MCP server (Streamable HTTP transport)
mcp>=1.8.0

# Recommendation engine
scikit-learn~=1.8.0
numpy~=2.4.3

# PDF generation
reportlab~=4.4.10

# Payment (optional — scanner works without it)
stripe~=15.0.0

# Database (optional — scanner works without it)
supabase~=2.28.3
